---
description: 移动端独立App、企业微信H5原型样式与浏览器兼容规范，Skill做移动端原型自动区分两套适配策略
globs:
  - "prototype/versions/**/*mobile*.html"
  - "prototype/versions/**/*wecom*.html"
  - "prototype/index.html"
alwaysApply: false
---
# 一、移动端两种场景区分说明
1. 独立App场景：用户回复「确认移动端」，生成完整iPhone16外壳沉浸式App；
2. 企微内嵌H5场景：用户回复「企微内嵌移动端」，不生成手机外壳、不做底部独立TabBar，适配企业微信内置浏览器环境；
3. 通用前置约束（两种场景全部生效）
产品依托公司统一内部平台开发，以下功能由平台统一封装提供，**移动端原型全部忽略，不设计、不绘制、不新增页面、按钮、Tab入口**：
1. 消息中心：消息通知、待办列表、已读未读、消息详情；
2. 流程审批：移动端发起审批、查看审批单、处理待审批、驳回/同意操作；
3. 角色管理、权限配置：移动端无需提供角色、权限相关操作入口；
原型只产出业务自身功能页面，不额外增加平台公共能力模块。

## 二、场景1：独立iOS App 规范（原逻辑保留不变）
### 1. 色彩规范
1. 骨架使用zinc中性色系，低饱和度，无刺眼亮色；
2. CTA按钮使用柔和品牌渐变，不使用纯纯色；
3. 毛玻璃组件：rgba(255,255,255,0.8) + backdrop-filter: blur(20px)；
4. 暗黑模式备选（按需），默认浅色清爽模式。

### 2. 尺寸、圆角、手机容器强制标准
1. 模拟iPhone16外框：宽390px、高844px；
2. 灵动岛：顶部居中 宽120px、高32px；
3. 手机外框圆角48px，边框#e5e7eb，外阴影柔和；
4. 卡片/按钮 rounded-2xl(24px)；弹窗 rounded-3xl(32px)；
5. 页面左右固定16px留白，内容不贴屏幕边缘。

### 3. 页面固定布局结构
1. 顶部：状态栏（时间、电量）+ 透明导航栏（返回、页面标题、业务操作按钮），无消息中心入口；
2. 中间内容：卡片流式列表、表单、详情、图文模块（仅业务功能）；
3. 底部固定磨砂TabBar：仅展示业务相关Tab，不添加审批、权限、消息Tab；
4. 弹窗从底部滑出，占屏幕75%高度，大圆角（仅业务弹窗，不含审批弹窗）。

## 三、场景2：企业微信内嵌H5专属规范（新增）
### 核心环境说明
功能仅作为企微工作台快捷入口，在企微内置浏览器打开，无独立App、无系统状态栏、无独立底部Tab栏，企微自带导航/消息/审批入口，原型不重复实现。
### 1. 布局强制要求
1. 不渲染iPhone手机外壳、灵动岛、手机边框；页面直接全屏展示H5；
2. 顶部仅保留业务导航栏，去除手机系统状态栏；
3. **禁止自定义底部TabBar**：企微自身底部自带导航，业务页面不新增底部标签；
4. 页面左右16px安全留白，适配企微浏览器不同机型；
5. 所有弹窗改为居中弹窗，禁止底部上滑弹窗（企微底部自带操作栏易冲突）。

### 2. 交互适配企微特性
1. 移除手机系统相关动效，仅保留基础按钮点击 scale-95；
2. 避免复杂毛玻璃、多层模糊（企微浏览器兼容性一般）；
3. 表单适配企微内置键盘，输入框底部预留安全距离；
4. 页面跳转仅使用页面内返回按钮，不依赖手机系统返回逻辑。

### 3. 特殊约束（企微专属）
1. 完全不设计消息、审批、角色、权限相关任何入口，全部交由企微+内部平台承载；
2. 不做App推送、桌面图标、灵动岛相关逻辑；
3. 页面轻量化，减少复杂动画，保证企微内加载速度。

## 4. 统一移动端交互规范（两种场景通用）
1. 按钮点击active:scale-95 按压缩小反馈；
2. Tab切换平滑过渡；
3. 列表可滚动，隐藏页面原生滚动条；
4. 输入框聚焦轻微上浮，软键盘适配预留底部空间。

## 5. HTML强制规范（双场景通用）
1. 视口固定配置：
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
2. 页面全部适配手机窄屏，禁止PC端宽表格；
3. 图片统一Unsplash生活化、简约产品图；
4. 所有弹窗、按钮文案简体中文。

## 6. 全局禁止行为（两种移动端场景统一）
1. 不生成PC侧边栏、多列表格；
2. 不使用小尺寸圆角、生硬黑色阴影；
3. 不铺满屏幕无留白；
4. 不出现大屏专属分页、复杂筛选栏；
5. 严禁绘制消息、审批、角色、权限相关页面、按钮、弹窗、底部Tab。