---
description: PRD撰写+多端原型产出整条流水线全局硬性规范，Skill执行相关任务必须优先遵从本文件约束
alwaysApply: true
---
# 一、顶层强制铁律（最高优先级，不可违反）
1. 禁止无意义前置追问：仅需求信息缺失时针对性提问，不重复、无范围乱提问；信息充足则直接生成PRD；
2. 严格两步流程，禁止跳过、颠倒：
   ① 用户只给想法/完善PRD → 第一阶段输出完整prd.md + GenUI确认表单
   ② 用户回复确认移动端/PC端/企微H5/组合端 → 第二阶段读取PRD生成对应HTML原型
3. 原型仅允许：HTML5 + Tailwind CDN + 原生JS，禁止Vue/React等编译框架；
4. 所有页面、PRD文案统一简体中文，无英文占位内容。

# 二、通用前置约束（PRD、原型全部生效）
## 内部平台公共能力说明
项目基于公司统一内部平台开发，以下能力由平台底层统一支撑，PRD、原型均不做设计、不产出页面：
1. 消息中心：站内通知、待办、消息推送、消息列表；
2. 流程审批：审批发起、审批节点、抄送、驳回、审批记录；
3. 角色管理、菜单/按钮/数据权限配置；
约束：
1. 撰写PRD时，无需设计上述模块需求、页面、交互流程；
2. 生成HTML原型时，不产出对应页面、弹窗、菜单、操作按钮；
3. 流程图、页面清单、功能模块中剔除审批、权限、消息相关内容，仅聚焦业务自有功能。

## 移动端两种交付形态区分（PRD设计必须区分）
1. 独立App形态：完整独立手机应用，带手机模拟外壳、自定义底部业务Tab栏；
2. 企业微信内嵌H5形态：仅企微工作台快捷入口，无独立App、无自定义底部Tab，复用企微原生导航、消息、审批能力，页面轻量化适配企微内置浏览器；
PRD需根据用户选择的终端形态，分开设计页面结构、交互逻辑，两套方案不混用。

## 3. 多版本管理强制规范
### 版本命名规则
版本统一格式：`v主版本.次版本.迭代号`，示例：v1.0.0、v1.1.2、v2.0.0
- v1.0.0：第一版基础版本
- v1.1.0：迭代新增功能版本
- v1.1.1：bug修复微调版本

### 版本生成约束
1. 用户需求明确版本号时，自动在对应`versions/版本号/`目录生成prd.md与原型；
2. 用户未指定版本号，默认新建v1.0.0目录存放全套文件；
3. 迭代更新需求（修改已有版本）：仅修改对应版本文件夹内文档/原型，不改动其他版本文件；
4. 重大改版需求：主动向用户确认是否新建更高版本文件夹；确认新建则完整复制旧版本内容后迭代修改，历史版本永久保留不可删除；用户选择不新建则在当前版本内迭代；
5. 禁止覆盖、删除历史版本目录，所有迭代版本永久留存，实现需求、原型全追溯；
6. PRD文档头部必须标注当前版本号、版本更新时间、版本迭代说明。
7. 每新增一个版本，同步更新根目录prototype/index.html总入口表格，新增该行版本全部跳转链接。

### 版本目录强制分层结构
/workspace/
├── docs/
│ └── versions/
│ ├── v1.0.0/
│ │ └── prd.md
│ ├── v1.1.0/
│ │ └── prd.md
│ └── v2.0.0/
│ └── prd.md
└── prototype/
├── index.html # GitHub 发布统一全局总入口，表格汇总所有版本、多端原型、PRD 链接
└── versions/
├── v1.0.0/
│ ├── index.html
│ └── pages/
├── v1.1.0/
│ ├── index.html
│ └── pages/
└── v2.0.0/
├── index.html
└── pages/

## 4. 全局总入口 prototype/index.html 强制规范
1. 页面用途：适配GitHub Pages静态发布，作为所有版本、多端原型、PRD文档统一访问门户；
2. 核心组件：固定表格，表格列固定为：版本号、版本迭代说明、PC端原型入口、独立App原型入口、企微H5原型入口、对应版本PRD文档；
3. 跳转逻辑：表格每个单元格均为可点击超链接，分别指向对应版本目录下原型index.html与docs/versions对应prd.md；
4. 页面样式：简洁轻量化，无复杂动画，兼容GitHub静态页面访问，使用基础Tailwind CDN；
5. 更新规则：每次新增/修改任意版本，自动刷新此总入口表格，完整保留全部历史版本条目，不删除旧版本表格行；
6. 页面顶部标注项目名称、原型仓库说明、GitHub访问提示。

# 三、第一阶段：PRD生成强制标准（路径/workspace/docs/versions/{版本号}/prd.md）
## PRD完整生成强制输出要求
**生成完整PRD文档**，写入 `/workspace/docs/prd.md`，内容必须包含以下全部模块，缺一不可：
1. 产品概述，必须包括产品定位、产品目标、用户角色定义
2. 业务流程板块，必须完整补齐4类Mermaid流程图：
   - 完整业务流程图 + 子业务流程图（flowchart）；
   - 用户交互流程图（flowchart）；
   - 流程状态机 stateDiagram-v2；
   - 系统数据流转时序图 sequenceDiagram；
3. 功能模块总览，严格两级结构，最小二级功能必须包含4项固定维度：
   - 功能介绍：一句话说明该功能核心价值；
   - 前置条件：用户操作前置、系统数据前置；
   - 数据权限：区分不同角色数据可见范围；
   - 页面跳转：按钮、操作对应的页面路由跳转逻辑；
4. 完整用户交互路径 User Flows，覆盖全部主流程、异常分支；
5. 页面清单与页面间跳转关系，梳理全部页面上下游跳转链路；
6. 非功能性需求，覆盖性能、安全、兼容性、可用性、可拓展性；
7. PRD头部固定版本信息区块，内置左侧悬浮可点击锚点目录，目录可快速定位各功能模块，适配GitHub Pages静态锚点跳转。

## PRD 完成后固定输出确认表单
```markdown
✅ **PRD与交互流程已生成**：`/workspace/docs/versions/{对应版本号}/prd.md`
-----------------------------------------------------------------------
📋 **GenUI Project Confirmation**
当前生成版本：【自动填充对应版本号，如v1.0.0】
-----------------------------------------------------------------------
请审核方案，并选择下一步操作：
[ 🚀 确认并生成移动端 App 原型 ]   (回复: "确认移动端")
[ 🧩 确认并生成企业微信内嵌H5原型 ] (回复: "确认企微H5")
[ 💻 确认并生成 PC 端 Web 原型 ]   (回复: "确认PC端")
[ 💻/🚀 确认并生成 PC 和APP 原型 ]   (回复: "两者都需要")
[ 💻/🧩 确认并生成 PC 和企微H5原型 ] (回复: "PC+企微H5")
[ 🛠️ 修改需求文档 ]               (回复: "修改: <您的意见>")
[ ❌ 拒绝并重写 ]                 (回复: "重写")
-----------------------------------------------------------------------
## 用户反馈处理规则
修改:xxx
局部微调：仅修改当前对应版本 PRD 对应章节，不影响其他版本；核心重构：在当前版本基础上迭代，或新建高版本；修改上限 3 次，超 3 次建议重新提交需求；
修改完成必须再次输出确认表单，标注当前操作版本；同步更新 prototype/index.html 全局总入口表格；
重写：清空当前版本原有 PRD，基于原始需求全新生成，输出确认表单；历史其他版本文件保留不动；同步更新全局总入口表格。

# 四、第二阶段：原型生成通用规范
## 1. 固定版本化目录结构
```plaintext
/workspace/prototype/versions/{版本号}/
├── index.html # 当前版本原型总入口，可预览本版本全部页面
└── pages/
    ├── 一级模块_二级模块_页面.html # 普通页面
    ├── 归属页面_弹窗功能.html # 业务弹窗独立文件
    轻量提示弹窗（成功 / 报错）无需单独 HTML，JS 动态渲染
## 2. 统一基础设计系统（PC/App/ 企微 H5 通用）
### 色彩
骨架使用 Zinc/Slate 中性色，仅 CTA 按钮用品牌色；禁用高饱和纯色 #0000FF
PC 端：B 端清爽留白；移动端 App：iOS18 毛玻璃沉浸式风格；企微 H5：轻量化简约，减少多层模糊

### 排版
字体栈：-apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", Roboto, "Helvetica Neue", sans-serif
单位 rem，正文行高≥1.6

### 尺寸质感基础标准
基础阴影：0 4px 12px rgba(0,0,0,0.05)；悬浮阴影：0 8px 24px rgba(0,0,0,0.08)
毛玻璃：backdrop-filter: blur(20px); background:rgba(255,255,255,0.8)；企微 H5 弱化模糊效果，保证兼容性

### 资源规范
图片：禁止 placeholder，统一 Unsplash 高清无水印图；图标使用 FontAwesome6 CDN

## 3. HTML 强制规范（三端通用）
固定引入 Tailwind、FontAwesome CDN；视口强制配置：
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
每个版本 index.html 页面顶部标注当前版本号，方便区分多版本。
# 五、原型交付自检（生成前逐条校验）
- [ ] 文档、原型存放路径符合版本分层目录规范，无跨版本混杂文件
- [ ] 根目录 prototype/index.html 全局总入口存在，表格包含全部历史版本链接
- [ ] PRD 头部、原型首页正确标注对应版本号与迭代说明
- [ ] 配色柔和无刺眼纯色，规范圆角、柔和阴影
- [ ] 全页面简体中文，无英文占位
- [ ] 图片来自 Unsplash，无裂图
- [ ] 文件命名符合规则，业务弹窗独立文件
- [ ] CDN 可正常加载，页面加载≤3s，适配 GitHub 静态页面访问
- [ ] 动效适配对应终端（PC/App/ 企微 H5 区分）
- [ ] 页面跳转、功能逻辑与同版本 prd.md 完全匹配
- [ ] 无消息中心、审批、角色、权限相关页面 / 按钮 / 弹窗
- [ ] 历史旧版本文件完整保留，未被覆盖、删除
- [ ] 全局总入口表格链接可正常跳转至对应版本 PRD、各端原型

# 六、禁止行为
1. 生成 PRD 前无理由主动追问用户
2. 跳过 PRD 直接生成原型
3. 使用占位图片、英文文本
4. 使用 React/Vue 等编译框架
5. PRD 缺失任意一类 Mermaid 流程图
6. PRD 生成后不输出完整确认表单
7. 随意修改确认表单文字选项
8. 原型 / PRD 中出现内部平台公共能力（消息 / 审批 / 角色 / 权限）相关模块
9. 不按 versions 版本目录存放文件，全部丢在 docs/prototype 根目录
10. 覆盖、删除历史版本目录，丢失迭代记录
11. 多版本文件混放，无版本隔离目录
12. 缺失 prototype/index.html 全局总入口文件
13. 新增版本后不更新全局总入口表格、删除历史版本表格行
14. 原型页面无法在 GitHub Pages 静态环境正常访问

## 三、修正核心说明
1. 修正顶层提问逻辑：改为「缺信息才提问」，不会无限循环追问；
2. 完善重大改版逻辑：明确用户确认新建版本的交互；
3. 标准化目录代码块缩进，Cursor 可识别路径；
4. 闭合 PRD 头部 markdown 代码块，修复语法解析失败；
5. 修正 rgba/blur 括号多余空格，前端渲染无异常；
6. 修复禁止行为序号断裂，完整 1-14 条；
7. 将确认表单单独封装代码块，结构分层清晰，AI 可完整输出表格。