---
description: PC端B端SaaS Web原型强制设计、CSS、布局规范，Skill生成/修改PC原型时严格套用
globs:
  - "prototype/versions/**/*pc*.html"
  - "prototype/index.html"
alwaysApply: false
---
# 一、PC端整体定位
B端后台/管理系统，清爽生产力风格，大留白、数据表格、多层菜单，适配大屏办公场景。

## 重要前置约束（内部平台公共能力）
本产品搭建在公司统一内部平台之上，以下能力由平台底层统一提供，**生成原型时完全不绘制、不开发、不设计、不新增页面/弹窗/菜单**：
1. 消息中心：站内通知、待办、消息推送、消息列表全部由平台提供；
2. 流程审批：审批流、发起审批、审批节点、审批记录、抄送、驳回、转办统一走平台；
3. 角色管理：角色新增/编辑/删除、角色分组；
4. 权限配置：菜单权限、按钮权限、数据行权限、字段脱敏权限；
原型仅做业务自身功能，无需设计上述公共模块入口、页面、弹窗、表单。

## 1. 色彩规范
1. 基础骨架：slate中性灰体系，禁用纯蓝#0000FF等高饱和纯色；
2. 主品牌色仅用于新增、提交、确认等CTA按钮；
3. 状态色：成功#10b981、警告#f59e0b、错误#ef4444、次要#64748b；
4. 背景：页面#f8fafc，卡片纯白#ffffff，侧边栏#1e293b暗黑侧边栏。

## 2. 尺寸圆角&阴影量化
1. 圆角：按钮 rounded-xl(20px)、卡片/弹窗 rounded-2xl(24px)；
2. 基础卡片阴影：0 4px 12px rgba(0,0,0,0.05)；
3. hover悬浮卡片：0 8px 24px rgba(0,0,0,0.08)；
4. 弹窗阴影：0 12px 40px rgba(0,0,0,0.12)。

## 3. 页面固定布局（强制Sidebar三栏结构）
1. 左侧固定侧边栏：仅展示**本业务专属模块菜单**，不增加角色、权限、审批、消息相关菜单；
2. 顶部Header：仅保留业务相关搜索、业务用户操作，消息、权限入口由平台自带，原型不绘制；
3. 右侧内容区：max-w-7xl居中，左右留白，不铺满超大屏幕；
4. 页面组件：筛选栏、数据表格、分页、弹窗表单、标签页Tabs（仅业务自有表单，不含审批、权限表单）。

## 4. 交互动效标准
1. 卡片hover：translateY(-4px)上浮+加深阴影；
2. 按钮hover：轻微加深品牌色；active轻微缩小scale-95；
3. 弹窗弹出淡入动画，关闭平滑淡出；
4. 表格行hover浅灰底色高亮，支持单选/多选。

## 5. HTML&页面规范
1. 技术栈：纯HTML + Tailwind CDN + FontAwesome6 + 原生JS；
2. 所有文字简体中文，无Lorem占位文本；
3. 页面命名规则：一级模块_二级模块_page.html；
4. 业务弹窗单独新建html，命名：归属页面_弹窗名称.html；
5. 首页index.html提供侧边菜单快速跳转所有页面，仅展示业务功能。

## 6. 图片、图标规范
1. 图标统一Heroicons/FontAwesome，与文字垂直居中；
2. 图表、配图使用Unsplash极简办公、数据类无水印图片；
3. 禁止placeholder占位图。

## 7. 自检约束
生成PC原型必须包含：侧边导航、表格分页、筛选表单、弹窗操作、hover交互、柔和阴影圆角；
禁止出现：消息中心、审批流程、角色管理、权限配置相关任何页面、按钮、弹窗、菜单。